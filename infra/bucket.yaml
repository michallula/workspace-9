apiVersion: storage.gcp.upbound.io/v1beta1
kind: Bucket
metadata:
  name: workspace-9-test-1
  namespace: workspace-9
  labels:
    docs.crossplane.io/example: provider-gcp
spec:
  forProvider:
    location: US
  providerConfigRef:
    name: default